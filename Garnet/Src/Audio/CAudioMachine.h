#pragma once

#include <memory>

namespace audio
{
	class CAudioClip;

	class CAudioMachine
	{
		//std::shared_ptr<CAudioClip> m_CurrentAudioClip;
	public:
		/*CAudioMachine();
		virtual ~CAudioMachine();

		void SetClip(const std::shared_ptr<CAudioClip>& Clip);

		void Play*/
	};
}